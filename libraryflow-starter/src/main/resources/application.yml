server:
  port: 8084

spring:
  application:
    name: libraryflow-starter
  datasource:
    url: jdbc:h2:mem:libraryflow-starter
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        hbm2ddl.create_namespaces: true
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
      path: /h2-console
